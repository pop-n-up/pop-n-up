spring:
  application:
    name: popnup

  profiles:
    active: local

  jpa:
    open-in-view: false

  # 네이버 지도 API 공동 설정 추가
  naver:
    maps:
      client-id: xuw6b5ypsr
      client-secret: xYpDojd2SU54of2IboHi0fIRD0Sr2J96LI9RKnYZ

  jwt:
    secret: ${JWT_SECRET}